<Window x:Class="Iris.MainWindow" Style="{StaticResource MaterialDesignWindow}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:local="clr-namespace:Iris"
        xmlns:pages="clr-namespace:Iris.Page"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <materialDesign:DrawerHost IsLeftDrawerOpen="{Binding ElementName=MenuToggleButton, Path=IsChecked}">
        <materialDesign:DrawerHost.LeftDrawerContent>
            <materialDesign:ColorZone Padding="16" Mode="PrimaryDark" DockPanel.Dock="Top" Width="250" VerticalAlignment="Stretch">
                <DockPanel>
                    <ToggleButton Style="{StaticResource MaterialDesignHamburgerToggleButton}"
                              DockPanel.Dock="Top" HorizontalAlignment="Right"
                              Margin="16" IsChecked="{Binding ElementName=MenuToggleButton, Path=IsChecked, Mode=TwoWay}" />
                    <Menu>
                        <Menu.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </Menu.ItemsPanel>
                        <MenuItem Header="Accueil" Foreground="White" Click="OpenHome" />
                        <MenuItem Header="Raccourcis" Foreground="White" Click="OpenShortcuts" />
                        <MenuItem Header="ChatGPT" Foreground="White" Click="OpenChatGPT" />
                        <MenuItem Header="UP Eogend" Foreground="White" Click="OpenMonitorEogend"  />
                        <MenuItem Header="UP Personnel" Foreground="White" Click="OpenMonitorPerso" />
                    </Menu>
                </DockPanel>
            </materialDesign:ColorZone>
        </materialDesign:DrawerHost.LeftDrawerContent>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>

            <materialDesign:ColorZone Padding="16" Mode="PrimaryDark" DockPanel.Dock="Top">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <ToggleButton Style="{StaticResource MaterialDesignHamburgerToggleButton}" IsChecked="False"
                                x:Name="MenuToggleButton" Grid.Column="0"/>
                    <Label Content="IRIS - ACCUEIL" x:Name="TitleLabel" Grid.Column="1" Style="{StaticResource MaterialDesignLabel}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="22" Foreground="White"/>
                </Grid>
            </materialDesign:ColorZone>

            <pages:HomePage x:Name="HomePage" Grid.Row="1" />
            <pages:ShortcutsPage x:Name="ShortcutsPage" Grid.Row="1" Visibility="Collapsed" />
            <DockPanel x:Name="ChatGPTPage" Visibility="Collapsed" Grid.Row="1">
                <wv2:WebView2CompositionControl Source="https://www.chatgpt.com" />
            </DockPanel>
            <pages:UptimePage x:Name="MonitorEogendPage" Visibility="Collapsed" Grid.Row="1" Title="Eogend" Slug="eogend" />
            <pages:UptimePage x:Name="MonitorPersoPage" Visibility="Collapsed" Grid.Row="1" Title="Personnel" Slug="perso" />
        </Grid>
    </materialDesign:DrawerHost>
</Window>
